git init
git remote add origin https://github.com/suhailchoudhary/test.git
git add .
if have readme or any other commite already in remote repo and the branch is same then 
git pull origin main --rebase

then 
git push origin main 


git push origin --delete branchname 
git branch branchname // to create new branch 
git branch -m branchname branchnamenew // to change the branch name in git 
git switch branchname // locally switching to branch
g

